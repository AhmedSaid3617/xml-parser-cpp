#ifndef COMPRESSION_H
#define COMPRESSION_H

#include <string>
#include <iostream>

/**
 * @brief Takes a string and removes all whitespaces and new lines to reduce size.
 */
std::string strip(std::string in_string);

#endif